receiver:
  type: http
  config:
    id: "789"
    host: 127.0.0.1
    port: 8080
    max_pending_requests: 100
processors:
  - type: add_field
    config:
      field: name
      value: gofire
exporters:
  - type: stdout

